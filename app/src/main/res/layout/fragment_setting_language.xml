<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/tools">
    <data>
        <variable
            name="toolbarVM"
            type="org.unimelb.itime.ui.viewmodel.ToolbarViewModel"/>
        <variable
            name="contentVM"
            type="org.unimelb.itime.ui.viewmodel.setting.SettingLanguageViewModel"/>
    </data>

<LinearLayout
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/whiteThree">
    <include layout="@layout/toolbar"
        bind:vm="@{toolbarVM}"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_divider_top_bottom"
        android:layout_marginTop="20dp"
        app:onItemClickListener="@{contentVM.onChooseLanguage()}"
        app:items="@{contentVM.items}"
        app:itemBinding="@{contentVM.itemView}"
        />

</LinearLayout>
</layout>
