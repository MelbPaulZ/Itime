<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:bind="http://schemas.android.com/apk/res-auto">
    <data>
        <variable
            name="contentVM"
            type="org.unimelb.itime.ui.viewmodel.SettingRegionViewModel" />

        <variable
            name="toolbarVM"
            type="org.unimelb.itime.ui.viewmodel.ToolbarViewModel" />
    </data>
<LinearLayout
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/fragment_background">
    <include layout="@layout/toolbar"
        bind:vm="@{toolbarVM}"/>

    <!--region list-->
    <org.unimelb.itime.widget.ITimeListView
        android:id="@+id/region_list"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        bind:items="@{contentVM.items}"
        bind:itemBinding="@{contentVM.itemView}"
        bind:onItemClickListener="@{contentVM.onClickRegion()}"/>

    <TextView
        style="@style/itime_bottom_divider_line" />



</LinearLayout>
</layout>
